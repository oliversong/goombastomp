/* Entry point for etherpad running on CloudFoundry
 * - this is the file run by CF and it has to be placed
 *   at the root of your app.
 */

var s = require("./node_modules/ep_etherpad-lite/node/server")
